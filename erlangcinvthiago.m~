clc;clear all;close all;

b = xlsread('ErlangCTable.xls');
delay = 0.4;
A = 2.5981;

linha = [];
coluna = [];
[l,c] = size(b);
n = [];
aux = [];
for i = 1:c
    if b(1,i) == delay
        for j = 2:l
            if b(j,i) == A
                n = j-1;
                break;
            else
                A - b(j,i);
            end
        end
    end
end

display(n);